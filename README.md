Add the github workflow for automatic build

### Create workflow yaml file
    - under the .github/workflows/just_build.yml
        - provide the name tag
        - mention the event upon which the workflow should start
        